import _ from 'underscore';

// 引入state和mutation
// import {reportState, reportMutation} from "./report";

var states = {};

// states = _.extend(states, reportState);

var mutation = {};

// mutation = _.extend(mutation, reportMutation);

export const baseState = states;
export const baseMutations = mutation;
